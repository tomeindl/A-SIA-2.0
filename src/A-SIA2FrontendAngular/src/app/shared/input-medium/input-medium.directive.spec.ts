import { InputMediumDirective } from './input-medium.directive';

describe('InputMediumDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMediumDirective();
    expect(directive).toBeTruthy();
  });
});
